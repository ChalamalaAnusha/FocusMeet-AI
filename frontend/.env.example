# Leave these unset when the friend opens the app through the host laptop's Vite URL.
# For a separately hosted frontend, point both values to the host laptop LAN address.
VITE_API_BASE_URL=/api
VITE_SOCKET_URL=
